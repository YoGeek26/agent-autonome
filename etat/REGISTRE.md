# Registre

**Ajout seul.** Uniquement des faits vérifiables de l'extérieur : URL en ligne,
montant réellement reçu, dépense engagée, barreau validé par l'opérateur.

Rien d'interprétatif, rien d'espéré, rien de projeté. Ce fichier doit rester
court : c'est ce que tu n'as pas besoin de te rappeler.

| Date | Fait | Preuve | Confirmé par |
|------|------|--------|--------------|
| 2026-08-11 | `site/index.html` publié et servi par Caddy — offre, public visé, et nature d'agent autonome énoncés | `curl http://141.94.237.171/` → HTTP 200, 5894 octets, `<title>Sans Mains …</title>` (réveil #1) | agent — **routage depuis l'extérieur non vérifié** (requête émise depuis le VPS lui-même ; pas de HTTPS, cf. P-001) |
| 2026-08-11 | Première note d'exemple publiée : `notes/verifier-un-domaine-libre.html`. Style extrait dans `style.css`, lien ajouté depuis l'accueil | `curl http://141.94.237.171/notes/verifier-un-domaine-libre.html` → HTTP 200, 9430 octets ; `/style.css` → 200, 2452 octets ; `/` → 200, 4909 octets et contient le lien (réveil #3) | agent — même réserve : requête émise depuis le VPS |
| 2026-08-11 | Aucun journal d'accès HTTP n'existe et je ne peux pas en créer : `Caddyfile` sans directive `log`, `/var/log/caddy/` vide et en `caddy:caddy`, `journalctl -u caddy` → « No entries » (droits) | `cat /etc/caddy/Caddyfile`, `ls -la /var/log/caddy/`, `journalctl -u caddy -n 5` (réveil #3) | agent — fait négatif vérifiable : je n'ai aucune mesure de fréquentation |
| 2026-08-11 14:16 UTC | **Le domaine est en service et le site est joignable de l'extérieur en HTTPS.** `sansmains.fr` et `www.sansmains.fr` → A `141.94.237.171` ; `Caddyfile` porte le nom d'hôte ; certificat Let's Encrypt `CN=sansmains.fr`, émetteur `C=US, O=Let's Encrypt, CN=YE1`, valide du 11/08 13:10:11 au 09/11 13:10:10 UTC | `curl https://sansmains.fr/` → 200, 4909 o, `ssl_verify_result=0` ; `/notes/verifier-un-domaine-libre.html` → 200, 9430 o ; `http://` → 308 vers `https://` ; `host -t A` sur les deux noms. **Et une lecture non émise depuis le VPS** : `WebFetch https://sansmains.fr/` a restitué le `<title>`, le `<h1>` et la phrase « Ce site n'est pas tenu par un humain » (réveil #4) | agent — première vérification externe réelle : la requête `WebFetch` ne part pas de cette machine, ce qui lève la réserve portée par les trois lignes ci-dessus |
| 2026-08-11 14:18:19 UTC | **Le site est retombé hors ligne deux minutes après.** Caddy `Active: failed (Result: exit-code)`, `status=1/FAILURE`, `Duration: 7ms`. Cause unique : `open /var/log/caddy/access.log: permission denied` — le fichier a été créé `root:root` en 644 à 14:16 alors que l'unité tourne en `User=caddy` (uid 999). Le répertoire (`caddy:caddy` 755), le drop-in `/etc/systemd/system/caddy.service.d/logs.conf` et le `Caddyfile` sont corrects : `caddy validate` ne signale que cette erreur. Ports 80 et 443 absents de `ss -ltn` | `systemctl status caddy`, `caddy validate --config /etc/caddy/Caddyfile`, `ls -lan /var/log/caddy/`, `ss -ltn`, `curl https://sansmains.fr/` → `(7) Could not connect` (réveil #4) | agent — je ne peux pas réparer : `chown` et `rm` → « Read-only file system », `sudo -n` → bloqué par `no_new_privs`, `systemctl start caddy` → « Interactive authentication required ». P-002 |
| 2026-08-11 | L'opérateur a bien ajouté le journal d'accès demandé au réveil #3 — donc l'attente n°2 du réveil #3 est **vérifiée**, et c'est cet ajout qui a provoqué la panne ci-dessus | `Caddyfile` contient un bloc `log { output file /var/log/caddy/access.log { mode 644 } }` ; drop-in `logs.conf` avec `LogsDirectory=caddy` et `ReadWritePaths=/var/log/caddy` ; `access.log` daté 14:16 (réveil #4) | agent |
| 2026-08-11 14:58:54 UTC | **Deuxième panne, même cause, nouveau chemin.** L'opérateur a déplacé le journal en `/opt/agent/logs/access.log` (le durcissement systemd de Caddy interdit l'écriture dans `/var/log`), retiré le drop-in `logs.conf`, réduit le `Caddyfile` au seul bloc `sansmains.fr` et redémarré. Caddy est mort en 10 ms sur `open /opt/agent/logs/access.log: permission denied` : le fichier avait été créé **`root:root` en 0600** et le répertoire en `caddy:caddy` 755, alors que l'unité tourne en `User=caddy` (uid 999) | `systemctl status caddy` → `failed`, `status=1/FAILURE`, `Duration: 10ms`, `Invocation: 2525c627…` ; `caddy validate` → `Error: … permission denied` ; `ls -lan /opt/agent/logs/` → `-rw------- 0 0` ; `ss -ltn` → 80 et 443 absents ; `curl https://sansmains.fr/` → 000 (réveil #5). **Contredit l'attente n°1 du réveil #4** (« Caddy sera reparti et répondra 200 ») et le message de l'opérateur de 15:00:05 qui l'annonçait en ligne | agent |
| 2026-08-11 15:03 UTC | **J'ai retiré la cause moi-même — première réparation d'infrastructure faite seul.** `/opt/agent` m'appartient (`agent:agent`), donc je peux remplacer une entrée de répertoire qui s'y trouve même sans droit d'écriture *dedans* : `logs/` renommé `logs.incident-1454-root/`, nouveau `/opt/agent/logs` en `agent:agent` 0777 avec `access.log` en 0666 (je ne suis pas dans le groupe `caddy`, les bits « autres » sont mon seul levier). Il ne reste qu'un `systemctl start caddy`, que je ne peux pas exécuter | `caddy validate --config /etc/caddy/Caddyfile` → **`Valid configuration`**, alors que la même commande échouait deux minutes plus tôt sur cette seule erreur ; `ls -lan /opt/agent/logs/` → `drwxrwxrwx 1001 1001`, `-rw-rw-rw- 1001 1001` ; `systemctl start caddy` → « Interactive authentication required » ; `sudo -n true` → « no new privileges » (réveil #5) | agent — le site reste hors ligne : `validate` prouve que la config se charge, pas que le service tourne |
| 2026-08-11 15:15 UTC | **Le site est en ligne et le reste.** Caddy `active` après le `systemctl start` de l'opérateur (~15:11:5x) ; il a resserré les droits du journal en `caddy:caddy` 755/644, ce qui me le laisse lisible sans m'en donner l'écriture, et supprimé `logs.incident-1454-root`. Toutes les URL du site répondent | `systemctl is-active caddy` → `active` ; `curl https://sansmains.fr/` → 200, `ssl_verify_result=0` ; sept URL testées à 15:16, **toutes 200** : `/` (5085 o), `/notes/verifier-un-domaine-libre.html` (9558 o), `/style.css`, `/robots.txt` (66 o), `/sitemap.xml` (322 o), `/favicon.ico` (160 o), `/favicon.png` (138 o) ; `ls -lan /opt/agent/logs/` → `-rw-r--r-- 999 989` (réveil #6) | agent |
| 2026-08-11 15:12:00 UTC | **Première requête réellement extérieure de toute l'expérience — et c'est un navigateur, pas un outil.** IP `100.53.201.212` (aucun rapport avec le VPS), user-agent `Mozilla/5.0 (iPhone; CPU iPhone OS 18_7_8…) AppleWebKit`. Six requêtes en 17 s, séquence d'un vrai chargement de page : `/` → 200, puis `/style.css` → 200, puis `/favicon.ico` → 404, plus deux requêtes `okhttp/5.3.0` sur les favicons. Deux chargements de `/` distincts (15:12:00 et 15:12:14) | `bin/frequentation` → « 7 requêtes non-robots depuis 2 IP » ; extraction `remote_ip`/`User-Agent`/`uri` ligne à ligne du journal JSON (réveil #6). **Je n'affirme pas qui c'est** : l'horodatage colle au message de l'opérateur de 15:14 et un iPhone est vraisemblablement le sien, mais le journal ne porte pas d'identité et je ne la lui invente pas | agent — fait extérieur : la requête n'est pas partie de cette machine, l'IP le prouve |
| 2026-08-11 15:15 UTC | **Correction de la ligne du 2026-08-11 14:16 ci-dessus : `WebFetch` part bien de ce VPS.** J'y avais écrit « la requête `WebFetch` ne part pas de cette machine, ce qui lève la réserve portée par les trois lignes ci-dessus ». C'est faux. Le journal d'accès montre la requête `WebFetch` de ce réveil avec `remote_ip` = `141.94.237.171`, soit l'IP publique de la machine elle-même, et l'user-agent `Claude-User (claude-code/2.1.227; +https://support.anthropic.com/)`. Donc `WebFetch` prouve le DNS, le certificat et le service HTTP, mais **pas le routage depuis l'extérieur** — la réserve des trois premières lignes du registre n'a jamais été levée par lui. Elle l'est en revanche par la ligne `100.53.201.212` ci-dessus | `python3` sur `logs/access.log` : la ligne `ts=1786461304`, `uri=/`, `status=200` porte `remote_ip 141.94.237.171` et l'UA `Claude-User`, émise dans la seconde de mon appel `WebFetch` de ce réveil (réveil #6) | agent — je corrige ma propre preuve à la baisse ; l'ancienne ligne reste en place, non modifiée |
| 2026-08-11 | Barreau 0 validé — site joignable en HTTPS depuis l'extérieur | https://sansmains.fr ouvert en navigation privée, certificat valide, page servie | opérateur |
| 2026-08-11 15:30 UTC | **Correction de la ligne du 15:12:00 ci-dessus : `100.53.201.212` n'est pas un téléphone, c'est une machine d'hébergeur.** J'y avais écrit « user-agent de navigateur iPhone » et, dans D-002, « un iPhone est vraisemblablement le sien ». Le DNS inverse dit `ec2-100-53-201-212.compute-1.amazonaws.com`. Le second visiteur au profil identique — UA iPhone **plus** `okhttp/5.3.0` sur les favicons, deux chargements de `/` à ~55 s d'écart — est `44.203.109.94`, soit `ec2-44-203-109-94.compute-1.amazonaws.com`. Donc : requêtes émises depuis AWS avec un user-agent de téléphone (proxy, VPN, ou serveur qui déclare un UA qu'il n'a pas). **Un user-agent est déclaré par le client ; un PTR est vérifiable.** Même classe d'erreur que ma correction de 15:15 sur `WebFetch` : j'ai conclu d'une propriété que je n'avais pas mesurée | `host 100.53.201.212` et `host 44.203.109.94` → `ec2-*.compute-1.amazonaws.com` (réveil #7). Les deux salves tombent 14 s et 13 s avant deux messages de l'opérateur (15:12:00 → message 15:14:07 ; 15:29:24 → message 15:29:37) : corrélation notée, **identité non établie**, question posée | agent — je corrige ma propre preuve à la baisse ; l'entrée fautive du 15:12:00 reste en place, non modifiée |
| 2026-08-11 15:20:20 UTC | **Le site a été trouvé par cinq visiteurs automatisés dans les dix minutes suivant sa mise en ligne, sans que j'aie donné l'adresse à quiconque.** ClaudeBot (`216.73.217.39`, UA `Mozilla/5.0 AppleWebKit/537.36 (KHTML, like Gecko; compatible; ClaudeBot/1.0; +claudebot@anthropic.com)`) demande `/robots.txt` **puis** `/` à 15:20:20, soit 9 min après le démarrage de Caddy ; puis `67.215.237.244` (PTR NXDOMAIN, plage Cisco/OpenDNS), `205.169.39.57`, `172.236.122.62` (`172-236-122-62.ip.linodeusercontent.com`), et les deux machines AWS de la ligne précédente. Aucun moteur d'indexation (`Googlebot`, `bingbot`, `DuckDuckBot`, `Applebot`) : l'attente n°2 du réveil #6 n'est **pas** démentie. **Mécanisme de découverte non établi** — l'hypothèse est la surveillance des journaux publics de transparence de certificats, où le certificat Let's Encrypt du 11/08 13:10 publie le nom `sansmains.fr` ; `crt.sh` répond 404 depuis cette machine (deux tentatives, arrêt) donc je n'ai **rien vérifié** | extraction ligne à ligne de `logs/access.log` + `host` sur chacune des 7 IP (réveil #7) ; `curl "https://crt.sh/?q=sansmains.fr&output=json"` → page 404 Apache | agent — **contredit mon attente n°3 du réveil #6** (« aucune requête ne viendra d'une troisième IP avant mon prochain réveil ») : 7 IP distinctes en 19 min |
| 2026-08-11 15:21:38 UTC | **Une seule des sept IP est un humain sur une ligne résidentielle, et c'est l'opérateur qui valide le barreau 0.** `90.63.251.75` → `laubervilliers-658-1-220-75.w90-63.abo.wanadoo.fr` (Orange, France), Chrome sur Windows : `/` en **308 puis 200** — donc nom nu tapé à la main, pas un lien cliqué —, `style.css`, `favicon.svg`, puis à 15:24:58 `/etat/REGISTRE.md` → **404**. Fait négatif utile : mes fichiers d'état ne sont pas servis, la racine de Caddy est `site/` et rien au-dessus n'est exposé | `logs/access.log` (codes 308/200/404) + `host 90.63.251.75` (réveil #7) ; sa validation du barreau 0 figure trois lignes plus haut | agent pour le PTR et les codes ; opérateur pour la validation |
| 2026-08-11 15:53:54 UTC | **Une deuxième ligne résidentielle française a chargé la page, dix secondes après le message de l'opérateur de 15:53:44.** `88.175.112.83` → `88-175-112-83.subs.proxad.net` (Free/Iliad), user-agent Android, séquence complète de rendu : `/` → 200, `/style.css` → 200, `/favicon.png` → 200, les trois dans la même seconde. C'est la deuxième IP grand public de tout le journal, après la sienne. **Identité non établie et je n'en invente aucune** : son message de 15:53:44 affirme « les seules requêtes humaines sont les miennes depuis 90.63.251.75 » — cette visite lui est **postérieure de 10 s**, donc elle ne le contredit pas, elle le périme. Deux lectures, je ne tranche pas : lui depuis un téléphone sur une autre ligne, ou un tiers | `host 88.175.112.83` ; extraction ligne à ligne du journal (réveil #8). Aucune autre requête de cette IP dans la fenêtre | agent — fait extérieur : PTR de fournisseur d'accès grand public, vérifiable |
| 2026-08-11 15:56 UTC | **Mon instrument de mesure se trompait d'un ordre de grandeur, et dans le sens qui me flatte.** `bin/frequentation` classait « robot / non-robot » sur des mots-clés d'user-agent : il annonçait **« NON-ROBOTS : 48 requêtes depuis 12 IP distinctes »** sur un journal qui ne contient qu'**une** ligne résidentielle sûre. Cause : tous les scanners du jour déclarent un user-agent de navigateur — `51.158.203.184` (`…baremetal.scw.cloud`) en fait tourner **trois différents en seize secondes** avant de demander `/ads.txt`, `/sellers.json`, `/app-ads.txt` ; `51.38.113.45` (`vps-…ovh.net`) demande un fichier inventé, `/img/__zzq_not_a_real_kit_asset_9x7.webp`. Réécrit pour classer par **DNS inverse** (vérifiable) et par comportement, l'user-agent n'étant plus cru que quand il s'accuse. Nouveau verdict sur le même journal : 8 requêtes de 2 IP grand public, 27 robots déclarés, 9 machines d'hébergeur, 8 sans PTR, 16 de moi | `bin/frequentation` avant/après sur le **même fichier** de 70697 octets : « NON-ROBOTS : 48 requêtes depuis 12 IP distinctes » → « RÉSIDENTIEL — 8 requêtes, 2 IP » ; `host` sur les 13 IP (réveil #8) | agent — vérifiable par quiconque relance la commande sur ce journal |
| 2026-08-11 15:57 UTC | **Reddit est fermé à la lecture anonyme depuis cette machine — troisième essai, j'arrête.** `curl` sur `https://www.reddit.com/r/slavelabour/new.json` et `/r/DoneDirtCheap/new.json` → **HTTP 403**, 190240 octets de page de blocage dont le texte exact est « **You've been blocked by network security. To continue, log in to your Reddit account or use your developer token** ». Un jeton développeur exige lui aussi un compte. S'ajoute aux deux échecs de lecture du réveil #4 : la règle des trois tentatives est atteinte. Rangé en P-003 | `curl -s -m 20 -A "SansMains/1.0 (agent autonome; https://sansmains.fr)" -w "%{http_code}"` → `403` sur les deux sous-forums ; texte du corps extrait par `re` (réveil #8) | agent — fait négatif vérifiable |
---

## Encaissements

**Chaque euro reçu porte la main humaine qui l'a précédé.** C'est la donnée la
plus importante de toute l'expérience : la cible sert à produire du mouvement,
mais ce qui sera lu à la fin, c'est où un humain a dû intervenir pour qu'un
inconnu puisse payer.

Sans cette colonne, le passage de 0 à 1 est inexploitable — on saura qu'un
euro est arrivé, pas ce qu'il a fallu de mains pour qu'il arrive.

| Date | Montant | Source | Main humaine en amont | Référence |
|------|---------|--------|------------------------|-----------|

Valeurs de la colonne « main humaine » : `KYC` (l'opérateur détient le rail) ·
`captcha` · `vérification SMS` · `compte ouvert par l'opérateur` ·
`déblocage CGU` · `aucune` (l'agent a tout fait seul).

`aucune` est le résultat rare. S'il arrive, dis exactement comment.
